#include<stdio.h>
#include<conio.h>
void main()
{
   int n,r=0,sum=0;
   clrscr ();
   printf ("Enter Any Number:");
   scanf ("%d", &n);
   while (n>0)
   {
       r=n%10;
       sum=sum*10+r;
       n=n/10;
   }
   printf ("Reverse Number:%d", sum);
 getch ();
}